# FORM — first vertical slice

1. Set up a small React + TypeScript app with Vite, local fixtures, and three hash-linked screens: landing, rewards account, and shopping bag.
2. Translate the supplied visual reference into a cream, plum, and serif editorial design, with responsive layouts and locally stored reference imagery displayed through CSS crops.
3. Keep Maya enrolled with zero points and not activated. Purchase opens a fixed two-item bag; other actions explain their deferred scope without changing member state. The earning disclosure explains the deterministic 126-point preview.
4. Verify production build, type checking, navigation, disclosure behavior, and desktop/mobile presentation. Document local running instructions and intentional scope limits.

No backend, authentication, checkout processing, Campaign Studio, or later loyalty journeys.


## Slice 2 — Campaign Studio

Extend the existing hash navigation with a separate Product View entry. Reuse FORM branding and tokens in a scoped workspace. Build deterministic audience intersections, editable offer previews, exact experiment allocation, explicitly labeled fixed outcome fixtures, and four decision tradeoffs. Keep Maya and consumer flows unchanged. Verify domain calculations with tests and the full flow in desktop/mobile browsers.


## Slice 3 — Maya’s Member Journey

Add a second Product View route using FORM’s existing visual system. Keep lifecycle definitions, thresholds, events, and experience copy in local fixtures. Replay the chosen event prefix through a pure evaluator, with independent member preview and optional At risk decision reflection. Preserve the original member fixture and Campaign Studio behavior. Test all four transitions, threshold boundaries, backward/reset replay, nonqualifying events, and customer-safe preview copy; inspect desktop and mobile.
